import React from 'react';
import { Dashboard } from '../Dashboard';

export const Statistics: React.FC = () => {
  return (
    <Dashboard>
      <p style={{ color: 'var(--black)' }}>Statistics</p>
    </Dashboard>
  );
};
